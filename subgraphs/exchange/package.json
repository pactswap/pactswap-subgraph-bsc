{
  "name": "pactswap-subgraph-bsc",
  "version": "1.0.0",
  "description": "PactSwap Subgraph BSC",
  "author": "PactSwap",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pactswap/pact-bsc subgraph.yaml"
  }
}
